$id: http://event.jupyter.org/test-simple
version: 1
title: Simple Test Schema
description: |
  Fails validation because the root level of this schema
  is missing redactionPolicies.
type: object
properties:
  prop1:
    title: Test Property 1
    description: |
      Test property 1.
    redactionPolicies:
      - unrestricted
    type: string
required:
  - prop1
